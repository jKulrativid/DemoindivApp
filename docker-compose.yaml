version: '3.8'

services:
  gateway_api:
    build: ./api
    command: uvicorn app.main:app --reload --host 127.0.0.1 --port 8080 
    volumes:
      - ./gateway_api/:/app/
    ports:
      - 8001:8080

  todo:
    build: ./todo
    command: uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
    volumes:
      - ./todo/:/app/
    ports:
      - 8002:8000
